# ~/.profile: executed by bash for interactive login shells (e.g., SSH sessions).
# Login shells do NOT read ~/.bashrc automatically, so source it here to keep
# aliases, PATH, and environment variables consistent across all sessions.

if [ -n "$BASH_VERSION" ]; then
    if [ -f ~/.bashrc ]; then
        . ~/.bashrc
    fi
fi
